#include "main.h"
/**
 *add - Entry point
 *@x:first_number
 *@y:second_number
 *Description: function that adds two integers and returns the result.
 *Return: x + y
*/

int add(int x, int y)
{
	return (x + y);
}
